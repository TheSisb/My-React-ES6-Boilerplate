import React from 'react';
import styles from './landing.css';

const Landing = React.createClass({
  render() {
    return (
      <div className={styles.test}>Landing</div>
    );
  }
});

export default Landing;
